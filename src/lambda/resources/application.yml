spring:
  main:
    web-application-type: NONE
  cloud:
    function:
      definition: helloFunction
      web:
        export:
          debug: true

logging:
  level:
    org:
      springframework:
        cloud:
          function: DEBUG
